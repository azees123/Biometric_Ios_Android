name: Build iOS App

on:
  push:
    branches: [ main ]

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      # Step 1: Checkout the repository
      - uses: actions/checkout@v4

      # Step 2: Set up Python (consider using 3.9 for better compatibility)
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      # Step 3: Set up Python Virtual Environment
      - name: Set up Virtual Environment
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip setuptools cython

      # Step 4: Install iOS Build Dependencies
      - name: Install iOS Build Dependencies
        run: |
          brew install autoconf automake libtool pkg-config
          source venv/bin/activate
          pip install buildozer pbxproj

      # Step 5: Cache Buildozer settings and pip downloads
      - name: Cache Buildozer and pip
        uses: actions/cache@v4
        with:
          path: |
            ~/.buildozer
            ~/.cache/pip
          key: ${{ runner.os }}-ios-build-${{ hashFiles('**/buildozer.spec') }}
          restore-keys: |
            ${{ runner.os }}-ios-build-

      # Step 6: Cache kivy-ios and hostpython3 builds
      - name: Cache Kivy iOS Build Environment
        uses: actions/cache@v4
        with:
          path: .buildozer/ios/platform/kivy-ios
          key: ${{ runner.os }}-kivy-ios-${{ hashFiles('**/buildozer.spec') }}
          restore-keys: |
            ${{ runner.os }}-kivy-ios-

      # Step 7: Ensure buildozer.spec exists
      - name: Ensure buildozer.spec exists
        run: |
          source venv/bin/activate
          test -f buildozer.spec || buildozer init

      # Step 8: Build the iOS app (debug)
      - name: Build iOS Debug App
        run: |
          source venv/bin/activate
          buildozer -v ios debug

      # Step 9: Upload built app artifacts
      - name: Upload iOS Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: iOS-Build-Files
          path: ./bin/

      # Step 10: Output Buildozer Logs on Failure
      - name: Output Buildozer Logs on Failure
        if: failure()
        run: cat ~/.buildozer/*.log || true
